export enum AlertType {
  INCIDENT = 'incident',
  UPDATE = 'update',
  RESOLUTION = 'resolution',
  WARNING = 'warning',
  SCHEDULED = 'scheduled'
}

export enum IncidentStatus {
  ACTIVE = 'active',
  RESOLVED = 'resolved'
}

export enum AlertStream {
  EARTH = 'earth',
  WATER = 'water',
  FIRE = 'fire',
  AIR = 'air'
}
